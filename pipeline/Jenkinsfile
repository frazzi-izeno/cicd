pipeline {
    agent { label 'your-label' }

    stages {
        stage('Test echo') {
            steps {
                echo "This stage is running on ${env.NODE_NAME}"
            }
        }
    }
}

